# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

package-lock.json
# bun is the canonical lockfile (CI/Nix). Don't commit other PMs' lockfiles —
# a stray pnpm-lock.yaml makes tauri-action run `pnpm` (not installed in CI).
pnpm-lock.yaml

node_modules
dist
dist-ssr
*.local
*.local.*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.claude/

/target/
recording_*
.crush/

# Models (large binaries excluded; VAD + vocab tracked so the release bundles them)
src-tauri/resources/models/*
!src-tauri/resources/models/silero_vad_v4.onnx
!src-tauri/resources/models/gigaam_vocab.txt

# Playwright
playwright-report/
test-results/
blob-report/

.direnv
.envrc

# Nix build output
result

# Local build helpers (machine-specific absolute paths)
/build-echo.bat
/dev-run-echo.bat

# Superpowers planning docs (kept local, not published)
docs/superpowers/

# Firecrawl skill artifacts
.firecrawl/
